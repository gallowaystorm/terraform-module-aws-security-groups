security_groups:
  - rule:
      name: "test-security-group"
      ingress_rules:
        - description: "Test description"
          is_self_source: "false"
          from_port: 80
          to_port: 80
          protocol: "tcp"
          cidr_blocks: ["0.0.0.0/0"]
        - description: "Test desciption 2"
          is_self_source: "false"
          from_port: 443
          to_port: 443
          protocol: "tcp"
          cidr_blocks: ["0.0.0.0/0"]
      egress_rules:
        - description: "Egress Test description"
          is_self_source: "false"
          from_port: 80
          to_port: 80
          protocol: "-1"
          cidr_blocks: ["0.0.0.0/0"]
